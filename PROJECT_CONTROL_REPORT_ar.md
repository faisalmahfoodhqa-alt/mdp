# تقرير الشامل — منصّة «توريد نت» (الويب + API فقط)

**التاريخ:** 5 مايو 2026  
**النطاق:** واجهة المتجر والباك اند الموصوفان في هذا المستودع. **لا يشمل** أي تطبيق جوال خارج هذا النطاق (مثل مشاريع منفصلة في مستودعات أخرى).

---

## 1. المقدمة والهدف

تلخيص **كيف تُدار منصّة التجارة**: ما يعمل في المتصفح (React)، وما يعمل على الخادم (Express + MongoDB)، ومسارات التحكم (عميل، بائع، إدارة، مشرف توصيل المنصّة عبر لوحة الويب)، **دون أي افتراض تكامل مع تطبيقات خارج هذا المستودع**.

---

## 2. لمحة عن المستودع

| الطبقة | المجلد | التقنية | الدور |
|--------|--------|---------|-------|
| **متجر الويب** | الجذر `src/` | React، Vite، Axios | واجهة العملاء والبائعين ولوحة الإدارة |
| **API** | `backend/` | Express، MongoDB، JWT، RBAC | مصدر الحقيقة عند تفعيل الاتصال |
| **التوثيق** | `backend/docs/`، ملفات أخرى بالجذر | Markdown/HTML | مرجع تقني وتقارير |

**هجين التخزين:** قد تعمل الواجهة جزئياً مع `localStorage` عند وجود أوضاع توافق؛ مع الـ API تُستخدم `VITE_USE_BACKEND` و`src/config/backend.js` و`src/api/backendApi.js`.

---

## 3. الواجهة (React + Vite)

- **`src/pages/`** — صفحات المتجر والدفع واللوحات.  
- **`src/context/`** — جلسة، سلة، مزامنة أوضاع متعددة.  
- **`src/api/`** — عميل Axios ونقاط الـ REST.  
- **`vite.config.js`** — وكيل `'/api'` إلى `127.0.0.1:5000` في التطوير لتفادي تعقيدات CORS.

متغيرات بيئة شائعة: `VITE_API_URL`، `VITE_USE_BACKEND` (التعطيل الصريح بـ `false`).

---

## 4. الباك اند (Express + MongoDB)

- التشغيل: `backend/` → `npm run dev`.  
- جذر الـ API: **`/api/v1`**؛ فحص **`GET /api/v1/health`** (مع تشغيل Mongo).  
- **أمان مختصر:** bcrypt، JWT، RBAC للأدمن، rate limiting عام.  
- **الطلبات:** إنشاء من العميل، تحديث حالة من البائع/الإدارة، ومزامنات محددة للإدارة (راجع الصلاحيات في الكود).

**توصيل عبر المنصّة:** منطق الواجهة ولوحة الإدارة (طابور، مشرف توصيل) يعمل ضمن نفس المنصّة؛ لا يُذكر هنا أي تطبيق جوال طرف ثالث.

---

## 5. نقاط التكامل داخل المنصّة فقط

1. **الويب ↔ Mongo API:** Axios + Bearer JWT.  
2. **البروكسي في التطوير:** الطلبات إلى `/api/v1/...` تُوجَّه إلى الباك اند.  
3. **المعرّفات:** تفضيل توحيد `id` كنص متوافق مع Mongo حيث ينطبق ذلك.

---

## 6. الأدوار (من جهة المنصّة)

| الدور | أين | ملاحظة |
|-------|-----|--------|
| عميل | الويب | تسوق، سلّة، طلبات |
| بائع | الويب | منتجات، إعدادات، طلباته |
| إدارة | الويب | مستخدمين، إعدادات، مزامنات ضمن الصلاحيات |
| مشرف توصيل المنصّة | الويب – لوحة الأدمن | طابور «توصيل توريد نت» وحالات الطلب ضمن RBAC |

---

## 7. مستندات مرتبطة بالمنصّة

| الملف | المحتوى |
|-------|---------|
| `backend/README.md` | تشغيل الـ API |
| `backend/docs/API.md` | مسارات REST |
| `backend/docs/FRONTEND_INTEGRATION.md` | ربط الواجهة والـ API |
| `backend/docs/PROJECT_ANALYSIS.md` | تحليل مخازن وحالات |

---

## 8. اقتراحات عملية (داخل نطاق المنصّة)

- توضيح **مصدر حقيقة واحد** للإنتاج (تقليل الاعتماد على `localStorage` للبيانات الحسّاسة).  
- تأمين **`JWT_SECRET`** وبيئة **`CORS_ORIGIN`** للإنتاج.  
- **نسخ احتياطي Mongo** ومتابعة أي مسارات تصدير تستخدمها الإدارة.  
- **اختبارات آلية** (واجهة/ـ API) عند القدرة على الاستثمار فيها.

---

## 9. الخلاصة

هذا التقرير يصف **منصّة تجارة ويب واحدة مع API اختيارية/إلزامية حسب الإعداد**. أي تطبيقات أو منتجات جوال خارج هذا المستودع **ليست جزءاً من هذا المستند** ولا تُفترض في التشغيل أو التخطيط الموثّق هنا.

---

*مُحدَّث ليعكس نطاق «المنصّة فقط» دون افتراض تكامل خارجي.*
